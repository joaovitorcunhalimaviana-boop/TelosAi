<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <!-- Subtle radial glow behind the text -->
    <radialGradient id="centerGlow" cx="50%" cy="48%" r="40%">
      <stop offset="0%" stop-color="#14BDAE" stop-opacity="0.08"/>
      <stop offset="100%" stop-color="#14BDAE" stop-opacity="0"/>
    </radialGradient>
    <!-- Clip for rounded corners -->
    <clipPath id="roundedClip">
      <rect width="512" height="512" rx="64" ry="64"/>
    </clipPath>
  </defs>

  <!-- Main rounded square background -->
  <g clip-path="url(#roundedClip)">
    <!-- Dark background -->
    <rect width="512" height="512" fill="#0B0E14"/>

    <!-- Center glow -->
    <rect width="512" height="512" fill="url(#centerGlow)"/>

    <!-- Subtle teal accent bar at the top -->
    <rect x="0" y="0" width="512" height="3" fill="#14BDAE" opacity="0.7"/>
  </g>

  <!-- Border -->
  <rect x="0.5" y="0.5" width="511" height="511" rx="64" ry="64" fill="none" stroke="#1E2535" stroke-width="1"/>

  <!-- VigIA text centered -->
  <text x="256" y="290" text-anchor="middle" font-family="'Cormorant Garamond', Georgia, 'Times New Roman', serif" font-size="120" font-weight="400">
    <tspan fill="#F0EAD6">Vig</tspan><tspan fill="#14BDAE" font-style="italic">IA</tspan>
  </text>
</svg>
